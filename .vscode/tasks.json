{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Format All",
      "type": "shell",
      "command": "npx prettier --write .",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": "build",
      "presentation": {
        "reveal": "silent",
        "panel": "shared"
      }
    }
  ]
}
